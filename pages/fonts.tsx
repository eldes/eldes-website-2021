import { NextPage } from 'next'
import Page, { PageSection } from '../components/Page'

const FontsPage: NextPage = () => {
	return (
		<Page title="Fonts" section={PageSection.Fonts}>
		</Page>
	)
}

export default FontsPage